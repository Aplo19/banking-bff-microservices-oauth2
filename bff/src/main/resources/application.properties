# Server Configuration
server.port=8086

# Application
spring.application.name=bff

# URLs de microservicios
microservicio.cliente.url=http://localhost:8085/api
microservicio.producto.url=http://localhost:8084/api
auth.server.url=http://localhost:9000

# Timeouts para WebClient
webclient.timeout=5000

encryption.secret-key=hDbSSWissKxXW0K5V7y92YKqJ8mqw5ZR0dwhm/0xrqc=
encryption.algorithm=AES/GCM/NoPadding
encryption.key-size=256
encryption.tag-length=128

spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:9000/oauth2/jwks
oauth2.token-uri=http://localhost:9000/oauth2/token

logging.level.com.banco.bff.service.EncryptionService=DEBUG
logging.level.com.banco.bff.controller=INFO
logging.level.com.banco.bff=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.oauth2=DEBUG

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

cors.allowed-origins=http://localhost:3000,http://localhost:8080
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# Credenciales para token interno del BFF
bff.client-id=bff-service
bff.client-secret=bff123
bff.token-uri=http://localhost:9000/oauth2/token
